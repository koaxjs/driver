{
  "name": "@koax/driver",
  "description": "Koax drivers.",
  "repository": "git://github.com/koaxjs/driver.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/channel": "^1.0.0",
    "@koax/fork": "^0.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-polyfill": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "koax": "^0.5.1",
    "tape": "^4.2.2"
  }
}
